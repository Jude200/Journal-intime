import 'package:flutter/material.dart';

class AppAbout extends StatelessWidget {
  const AppAbout({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("A propos"),),
    );
  }
}